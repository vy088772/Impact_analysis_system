# 🧩 進階手冊 — 專案結構、API、設定與維運

本文件收錄**不在日常操作範圍**、但維護／除錯／整合時會用到的內容：完整專案結構、HTTP API 清單、環境變數全表、內容品質控制、external wrapper contract 維護流程。日常操作（啟動服務、跑分析、更新快取）請看 [使用說明書.md](./使用說明書.md)；內部資料流程圖見 [流程圖.md](./流程圖.md) 與 [流程圖_進階.md](./流程圖_進階.md)。

> 🔖 **文件對照基準**：`Impact_analysis_system` 的 `6254661`、`llamaindex-spec-rag` 的 `2bfe29e`（都是 2026-08-31）。完整說明與下次更新的比對指令，見 [使用說明書.md § 文件對照基準](./使用說明書.md#-文件對照基準更新文件前先看這裡)。

## 📑 目錄
- [兩個入口：main.py 與 service/api.py](#兩個入口mainpy-與-serviceapipy)
- [專案結構](#專案結構)
- [HTTP API 完整清單](#http-api-完整清單)
- [環境變數與設定](#環境變數與設定)
- [內容品質與檢索控制（spec-rag 端）](#內容品質與檢索控制spec-rag-端)
- [一次性維護工具](#一次性維護工具)
- [External wrapper contract 維護](#external-wrapper-contract-維護)
- [常見問題（進階）](#常見問題進階)
- [延伸閱讀](#延伸閱讀)
- [設計重點](#設計重點)

---

## 兩個入口：main.py 與 service/api.py

`main.py` 是**另一個獨立的舊版互動式選單 CLI**（測試設定／掃描專案／智慧搜尋／生成依賴圖／分析 SP），跟 `service/api.py`（FastAPI 服務，主要工作流程的真正入口）彼此不互相呼叫。使用說明書描述的完整流程（Impact 服務 + spec-rag 的 `run_cli`/`refresh_cli`）全部走 `service/api.py`；如果只是要做「變更影響分析」的日常操作，不需要用到 `main.py`，它比較適合單機除錯或不想啟動服務時的臨時查詢。

---

## 專案結構

```
Impact_analysis_system/
├── .env                          # 機密設定（Azure PAT、服務埠號等，不進版控）
├── main.py                       # 獨立的舊版互動式 CLI（見上方說明），非主要工作流程入口
├── config/
│   ├── settings.py                # 全域設定（讀 .env）＋資料庫連線設定
│   ├── sp_detector_config.py       # 預存程序偵測規則設定
│   └── sp_detection_rules.json
├── code_analyzer/                 # 靜態分析核心（無 AI）
│   ├── project_scanner.py          # 掃描入口：依框架偵測結果分派給各解析器
│   ├── project_type_detector.py    # 偵測專案框架（WebForms/MVC/Vue…）
│   ├── csharp_parser.py            # C# 方法/類別/呼叫關係解析
│   ├── aspx_parser.py              # ASPX/ASCX 解析：控制項、GridView 欄位分組、事件
│   ├── razor_parser.py             # Razor .cshtml 解析
│   ├── vue_parser.py               # Vue 元件解析
│   ├── sql_analyzer.py             # SQL Server object dump、定義與 Schema 讀取
│   ├── csharp_analysis_gateway.py  # C# invocation 與 SQL Execution Graph 的 evidence seam
│   ├── external_wrapper_contracts.py # external wrapper contract registry 模型、implementation identity 比對
│   ├── db_connection_tracker.py    # DB 連線字串／資料表存取追蹤
│   ├── dependency_graph.py         # 呼叫鏈/依賴圖
│   ├── azure_fetcher.py            # Azure DevOps clone/pull
│   ├── smart_file_finder.py        # 依程式名定位檔案
│   ├── config_parser.py            # 解析 web.config / appsettings.json 連線字串
│   ├── report_generator.py         # HTML 互動報告 + Chart.js 圖表（main.py 用）
│   ├── static_analyzer_host.py     # 呼叫 tools/StaticAnalyzerHost（獨立 C# 子專案）的 client
│   └── models.py                   # 靜態分析共用資料模型（FileAnalysisResult 等）
├── service/                        # FastAPI 服務層（供 spec-rag 呼叫）
│   ├── api.py                       # 10 個 HTTP endpoint，見下方清單
│   ├── analyze_service.py           # 主分析流程：Gateway join、Graph readiness、path 與反查，以及 locate_object（只讀索引）
│   ├── scan_store.py                # 掃描結果持久化快取（pickle + _CACHE_VERSION）
│   ├── sql_cache_store.py            # SQL object + Graph 本機快取（JSON + version/scope validation），並建置 Object Location Index
│   ├── sql_execution_graph.py       # typed SQL nodes/relationships、CALL 與 terminal operations
│   ├── execution_path_builder.py    # 建立 full/compact Execution Paths 與 path_id
│   ├── graph_queries.py             # query_table_accesses() 等 typed Graph query
│   ├── flow_chain_builder.py        # /flow_chain 端點的實作（forward/backward 組鏈）
│   ├── dependency_fetcher.py        # SQL Execution Graph 的相容性 adapter
│   ├── sp_call_fetcher.py           # SP 內部巢狀 EXEC 呼叫比對
│   ├── refresh_progress.py          # refresh_sql job 的 thread-safe progress state
│   ├── repo_manager.py              # 依 catalog 來源 clone/沿用本機 repo（path 支援單一字串或多子資料夾清單）
│   ├── snippet_extractor.py         # 依方法位置擷取程式碼片段
│   ├── call_chain_builder.py        # 組出呼叫鏈
│   ├── fk_resolver.py               # 外鍵連動資料表查詢
│   ├── sp_fetcher.py                # SP 完整定義（讀已驗證的本機 SQL 快取）
│   ├── view_fetcher.py              # SQL View 完整定義（只讀本機 SQL 快取）
│   ├── udf_fetcher.py               # UDF 完整定義（依程式內嵌 SQL 文字比對本機 UDF 名單）
│   ├── reference_expander.py        # 跨程式呼叫參照展開（類似「跟隨參照」）
│   ├── contract_preflight.py        # refresh-time contract 正規化與 staged onboarding 判斷
│   ├── contract_transaction.py      # external wrapper contract registry/catalog 的 atomic two-file commit
│   ├── contract_acceptance.py       # 人工審核 external wrapper contract proposal 的 preview/apply
│   ├── migration_report.py          # migration cutover report／regression matrix（review artifact，非正式資料來源）
│   └── schemas.py                   # 請求/回應 Pydantic 模型
├── tools/
│   ├── list_azure_repos.py           # 列出 Azure DevOps 可用 repo（除錯用）
│   ├── check_data_status.py         # 盤點各系統的規格書/程式碼/SQL 資料整理狀態（除錯用）
│   ├── discover_external_wrappers.py # 盤點 external wrapper 呼叫並比對 contract registry（唯讀）
│   ├── coverage_report.py            # 每個 System 兩個解析比例與線以下的理由代碼（唯讀，重用快取）
│   ├── accept_external_wrapper_contract.py # 審核後 external wrapper contract proposal 的 preview/apply CLI
│   ├── backfill_object_location_indexes.py # 一次性：幫舊 SQL 快取補建 Object Location Index
│   ├── repair_sql_execution_graphs.py     # 一次性：重建舊快取的 SQL Execution Graph（修正 offset）
│   ├── migrate_sql_cache_keys.py          # 一次性：舊 system_id 式快取檔名改成 (server, database, schema) 鍵
│   ├── export_openapi_schema.py           # 從 service.api:app 導出 OpenAPI schema 到 docs/openapi/openapi.json
│   └── StaticAnalyzerHost/           # 獨立 C# console 子專案，供 static_analyzer_host.py 呼叫
├── data/
│   ├── repos/                       # clone 下來的原始碼（依 project/repo/path）
│   ├── scan_cache/                  # 靜態掃描結果快取（*.pkl + *.meta.json）
│   ├── sql_cache/                   # SQL 物件與 SQL Execution Graph 快取（*.json、*.meta.json、*.index.json）
│   └── code_embeddings/             # 程式碼 embedding／注記快取（USE_CODE_EMBEDDING）
├── output/
│   └── impact_analysis/             # AI 報告（impact_*.md）＋AI 輸入存底（ai_input_*.txt）
├── docs/                            # 見「延伸閱讀」
│   └── openapi/openapi.json         # 由 tools/export_openapi_schema.py 自動產生，不手改
└── tests/                           # 驗證腳本
```

> 協作方 `llamaindex-spec-rag` 的呼叫端模組位於 `impact_orch/`：`rag_client.py`（HTTP 客戶端）、`program_extractor.py`（萃取程式名）、`context_builder.py`（組裝 AI 上下文）、`ai_reasoner.py`（呼叫 OpenAI）、`grounding.py`（落地驗證）、`run_cli.py`/`refresh_cli.py`/`refresh_sql_cli.py`（互動式 CLI）、`sp_lookup.py`/`table_lookup.py`（SP／資料表反查）、`agent_tools.py`（agent 工具集）、`orchestrator.py`/`path_selection.py`（shared path selection）、`finalization.py`（selected evidence 組裝與 final answer）、`discovery_scope.py`（agent 模式合併候選 system/program 的資料結構）、`scope_policy.py`（exact lookup 的 scope 判斷）、`source_resolver.py`（由 catalog 解析 Impact 來源）、`code_retriever.py`（embedding 語意檢索）、`flow_chain_budget.py`（`get_flow_chains` 呼叫預算）、`cross_system_lookup.py`（跨系統 cached program 比對）、`output_writer.py`（寫出報告檔）。>
> 2026-08 新增的 `impact_orch/` 模組：`candidate_database_set.py`（把 Lookup Database Set 與 `/locate_object` 結果交集成 Candidate Database Set）、`sql_cache_identity.py`（`(server, database)` 身分正規化）、`request_budget.py`（全次分析共用的 HTTP 請求上限與短暗重試）、`evidence_sufficiency.py`（證據充分性迴圈）、`evidence_report.py`（報告末尾的證據蒐集附錄）、`explore_routing.py`（問題文字分類成 SP／資料表／程式代號候選）、`spec_segments.py`（Indexed Specification Segment 的組裝與排序）、`terminal_menu.py`（上下鍵選單）。
---

## HTTP API 完整清單

不經 spec-rag、直接打 Impact 服務時使用（除錯用）：

| Method | Path | 用途 |
|---|---|---|
| GET | `/health` | 健康檢查 |
| POST | `/analyze` | 對指定程式跑靜態分析，回傳方法/SP/資料表/呼叫鏈/Execution Paths |
| POST | `/refresh` | 更新程式碼快取（git pull + 重新解析），對應 `refresh_cli` |
| POST | `/refresh_sql` | 更新 SQL 快取（dump 物件 + 建 Graph + 寫 Object Location Index），對應 `refresh_sql_cli` |
| GET | `/refresh_sql/status/{job_id}` | 輪詢 `/refresh_sql` 的 job 進度 |
| GET | `/scan_records` | 列出本機每份 SQL 快取的 Scan Record（伺服器、資料庫、schema、上次掃描時間） |
| POST | `/find_by_sp` | 以 SP 名稱反查有呼叫它的程式 |
| POST | `/find_by_table` | 以資料表名稱反查有存取它的程式（含 `access_type`/`write_only`） |
| POST | `/locate_object` | 只看 Object Location Index，回答「哪幾個資料庫可能持有這個名稱」；完全不開 SQL 快取，供反查前先縮小範圍 |
| POST | `/flow_chain` | 組出 forward（UI 操作→資料表）或 backward（資料表→程式）候選鏈 |
| POST | `/path_evidence` | 取得單一 `path_id` 的 source-backed 完整證據 |
| POST | `/wrapper_contracts/accept` | 審核並套用 external wrapper contract proposal（對應 `tools/accept_external_wrapper_contract.py` 的邏輯，該 CLI 直接呼叫 `service/contract_acceptance.py`，未必經過這支 API） |

> 完整的請求／回應欄位以 `docs/openapi/openapi.json` 為準。該檔由 `python -m tools.export_openapi_schema` 從實際路由導出，**不手改**；新增或修改任何路由後必須重新導出，否則 `tests/test_export_openapi_schema.py` 會失敗。用 `--check` 可只檢查不寫檔。

### 範例：`/locate_object`

```powershell
$locReq = @{ object_name = "SOrder"; kind = "table" } | ConvertTo-Json
Invoke-RestMethod -Uri http://127.0.0.1:8800/locate_object -Method Post -Body $locReq -ContentType "application/json"
```

`kind` 只接受 `sp`（對應 procedure/view/function 名稱）或 `table`，其他值回 HTTP 400。回應的 `matched` 是「索引新鮮且持有這個名稱」，`unindexed` 是「索引缺席或過期，服務無法代答，請自己整份讀」；**兩份清單都沒出現的資料庫就是剪枝結果**，呼叫端可以直接跳過。每個 `(server, database)` 至多出現一次、且只在其中一份清單裡。詳見 `docs/adr/0012-object-location-index-authoritative-pruning.md`。

### 範例：`/analyze`

```powershell
$body = @{
  system = "Y-Docs_TTPUR"
  source = @{ project = "System Dept 1"; repo = "Y-DOCs"; branch = ""; path = "TTPUR" }
  program_names = @("PUR_CostUpload", "PUR_YMMCPriceImport.aspx")
  database = "Y-Docs_TTPUR"
  db_server = "SQLSRV01"
  db_name = "PUR_Production"
  include_snippets = $true
  include_sp_defs = $false
  include_view_layer = $true
  include_execution_paths = $true
  question = "取消一筆採購單時會寫到哪些表？"
  fk_depth = 0
  refresh = $false
} | ConvertTo-Json

Invoke-RestMethod -Uri http://127.0.0.1:8800/analyze -Method Post -Body $body -ContentType "application/json"
```

`question` 是選填的分析問題原文，只用來排 Compact Path Candidates 的相關性順序，**不會改變任何 evidence 判定**；留空則依預設順序回傳。

### 範例：`/refresh`、`/refresh_sql`

```powershell
$r = @{ system="Y-Docs_TTPUR"; source=@{ project="System Dept 1"; repo="Y-DOCs"; branch=""; path="TTPUR" } } | ConvertTo-Json
Invoke-RestMethod -Uri http://127.0.0.1:8800/refresh -Method Post -Body $r -ContentType "application/json"

$sqlReq = @{ database = "Y-Docs_TTPUR"; server = "SQLSRV01"; db_name = "PUR_Production"; db_schema = "dbo"; job_id = "manual-Y-Docs_TTPUR-002" } | ConvertTo-Json
Invoke-RestMethod -Uri http://127.0.0.1:8800/refresh_sql -Method Post -Body $sqlReq -ContentType "application/json"
Invoke-RestMethod -Uri http://127.0.0.1:8800/refresh_sql/status/manual-Y-Docs_TTPUR-002
```

### 範例：`/find_by_sp`、`/find_by_table`

```powershell
# 以 SP 名稱反查（不知道屬於哪個系統時，逐系統呼叫）
$spReq = @{ source = @{ project = "System Dept 1"; repo = "Y-DOCs"; branch = ""; path = "TTPUR" }; sp_name = "usp_POPublish"; database = "Y-Docs_TTPUR"; cache_only = $true } | ConvertTo-Json
Invoke-RestMethod -Uri http://127.0.0.1:8800/find_by_sp -Method Post -Body $spReq -ContentType "application/json"

# 以資料表名稱反查，write_only=true 只取得寫入類命中
$tblWriteReq = @{ source = @{ project = "System Dept 1"; repo = "Y-DOCs"; branch = ""; path = "TTPUR" }; table_name = "SOrder"; cache_only = $true; database = "Y-Docs_TTPUR"; write_only = $true } | ConvertTo-Json
Invoke-RestMethod -Uri http://127.0.0.1:8800/find_by_table -Method Post -Body $tblWriteReq -ContentType "application/json"
```

`cache_only=true`（預設）時只比對已快取的掃描結果，不觸發 clone；提供 `database` 後正式反查需通過 database-scoped Graph readiness，缺失/過期回 HTTP 409（code `sql_execution_graph_required`）。比對表名時會正規化（去掉 `dbo.`/中括號等 schema 前綴）。命中不只一個系統時，代表可能是同名巧合，不會自動合併，呼叫端（CLI/agent）應反問使用者確認。

### 範例：`/path_evidence`

```powershell
$pathReq = @{
  path_id = "Y-Docs_TTPUR:path:example"
  system = "Y-Docs_TTPUR"
  source = @{ project = "System Dept 1"; repo = "Y-DOCs"; branch = ""; path = "TTPUR" }
  program_names = @("PUR_SOQry.aspx")
  database = "Y-Docs_TTPUR"
} | ConvertTo-Json

Invoke-RestMethod -Uri http://127.0.0.1:8800/path_evidence -Method Post -Body $pathReq -ContentType "application/json"
```

回傳 `source_span`、`sp_chain`、`operations`、`branch_context`、`evidence`、`reason` 與 unresolved targets。找不到 `path_id` 回 404；Graph 缺失/過期或 snapshot 不一致回對應的 409。

---

## 環境變數與設定

### Impact 服務的 `.env`

```env
AZURE_DEVOPS_ORG=topmost
AZURE_DEVOPS_PAT=<你的 PAT，需要 Code:Read 權限>
AZURE_DEVOPS_BRANCH=            # 留空用 repo 預設分支

SERVICE_HOST=127.0.0.1
SERVICE_PORT=8800
AZURE_CLONE_ROOT=./data/repos
SCAN_CACHE_ROOT=./data/scan_cache
SQL_CACHE_ROOT=./data/sql_cache   # SP/View/Function/資料表 Schema 的本機落地快取根目錄
```

帳號、密碼、連接埠、加密等 SQL Server **連線共用參數**（`DB_*`）也在這裡；每個系統實際依賴哪些 (伺服器, 資料庫)，由 spec-rag catalog 的 `databases`（一組 `{server, name}` 清單）逐系統標注，實際登錄哪些伺服器、資料庫則另外記在 spec-rag 的 `SQLServerData.json`（見下）。

### spec-rag 的 catalog（`catalog/system_catalog.json`）

```jsonc
{
  "system_id": "Y-Docs_TTPUR",
  "azure": { "project": "System Dept 1", "repo": "Y-DOCs", "branch": "", "path": "TTPUR" },
  "databases": [
    { "server": "SQLSRV01", "name": "PUR_Production" }
  ]
}
```

- `azure.repo` 為空的系統無法分析（已停用讀取本機任意路徑）。
- `azure.path` 支援**陣列**（如 `["TTPUR", "ATV", "Notification", "Response"]`），用於同一邏輯系統橫跨同一 repo 底下多個子資料夾的情況；Impact 會逐一 clone/掃描再合併成單一邏輯結果。
- `databases` 為空清單的系統無法使用 SP 完整定義／View 定義／FK 連動表，`/refresh_sql` 會直接拒絕連線；`/analyze` 對應部分安全回空、不影響其餘分析。第一筆宣告即該系統自己的 SQL 快取來源；要掃描該系統宣告依賴的每個資料庫，用 `refresh_sql_cli --system_id <system_id>`。

### spec-rag 的 `.env`

```env
IMPACT_SERVICE_URL=http://127.0.0.1:8800
IMPACT_SERVICE_TIMEOUT=300
IMPACT_SQL_REFRESH_TIMEOUT=1800   # 只給 SQL 快取更新用的逐時秒數（掃整庫很慢，與一般請求分開）
IMPACT_FK_DEPTH=0
EVIDENCE_REQUEST_CEILING=60       # 單次分析共用的 HTTP 請求上限（analyze/flow_chain/path_evidence 含重試）

# 影響分析 AI 控制（AI 跑在 spec-rag，所以放這裡，不是 Impact 的 .env）
RUN_AI=true                # false → 不呼叫 OpenAI（省 token），只做靜態分析與組裝
SEE_AI_PROMPT=false        # true  → 寫出/顯示「送進 AI 的完整 prompt」供檢視

# 內容品質 / 檢索
INCLUDE_SP_DEFS=false        # 連資料庫撈 SP 完整定義；需 DB
INCLUDE_VIEW_LAYER=false     # 是否回傳 View 層資訊（GridView 欄位分組/查詢條件/前端 script）
CONTEXT_MAX_CODE_CHARS=24000 # 送進 AI 的程式碼總量上限
CONTEXT_MAX_SPEC_CHARS=20000 # 送進 AI 的規格書內容總量上限
USE_CODE_EMBEDDING=false     # 用 embedding 語意檢索挑相關方法（中文問題也準）
CODE_EMBED_TOP_K=6           # 檢索取最相關的前幾個方法
CODE_EMBED_MIN_SIM_RATIO=0.85 # 相對相似度門檻（見下方說明）
CODE_EMBED_MIN_ABS_SIM=0.15   # 絕對相似度下限
SPEC_SEGMENT_MIN_SIM_RATIO=0.93 # 只給規格書分段排序用的相對門檻（比上面的通用值嚴）
USE_CODE_ANNOTATION=false    # 先用便宜模型把每個函式注記再檢索
IMPACT_EXPAND_DEPTH=0            # 跨程式參照展開層數
IMPACT_EXPAND_MAX_PROGRAMS=0     # 跨程式參照展開上限

# Agent 模式（模式 4／5）
AGENT_MAX_ANALYZE_CALLS=3    # agent 最多呼叫幾次 /analyze
AGENT_MAX_FLOW_CHAIN_CALLS=3 # agent 最多呼叫幾次 /flow_chain
AGENT_MAX_SUFFICIENCY_ROUNDS=3 # 證據充分性迴圈最多跑幾輪（每輪都重跑一次 agent）
AGENT_TIMEOUT=90             # agent 單次判斷逾時秒數
AGENT_ROUTER_MODEL=          # 留空 fallback 用 OPENAI_LOW_MODEL

# 模型
OPENAI_MODEL=gpt-5.4                       # 最終整合答案
OPENAI_LOW_MODEL=gpt-5.4-mini              # 片段摘要/router fallback
OPENAI_EMBEDDING_MODEL=text-embedding-3-small
```

> `RUN_AI`/`SEE_AI_PROMPT` 由 `.env` 強制決定，忽略程式呼叫時傳入的 `run_ai=`/`dump_ai_input=`，避免不同呼叫路徑（CLI/agent/API）行為不一致。

---

## 內容品質與檢索控制（spec-rag 端）

| 設定 | 作用 | 成本 |
|------|------|------|
| `INCLUDE_SP_DEFS` | 從已驗證 SQL cache 帶入 SP 完整 T-SQL、參數與實際碰的表；只列出被挑中方法有引用到的 SP | 需先跑 `refresh_sql_cli`；不花 token |
| `CONTEXT_MAX_CODE_CHARS` / `CONTEXT_MAX_SPEC_CHARS` | 送進 AI 的程式碼／規格書總量上限；超過的內容只列名不展開全文 | — |
| `USE_CODE_EMBEDDING` | 把方法向量化建索引（依程式碼版本快取），中文問題也能準確命中相關方法 | 索引一次 + 每次一個 query embedding，極便宜 |
| `CODE_EMBED_TOP_K` / `CODE_EMBED_MIN_SIM_RATIO` / `CODE_EMBED_MIN_ABS_SIM` | 三重篩選：先取前 K 個候選，再用「相對本程式最相關方法的相似度比例」與「絕對相似度下限」濾掉真正不相關的方法，避免方法數只比 K 多一點時幾乎全選 | — |
| `USE_CODE_ANNOTATION` | 先用便宜模型把每個函式/畫面區塊/SQL 定義注記成一句中文摘要再檢索（中文 vs 中文更準），送給 AI 的仍是原始程式碼 | 一次性注記成本，依內容雜湊快取 |
| `INCLUDE_VIEW_LAYER` | 回傳 ASPX/View 層擷取的欄位、GridView 分組、查詢條件與前端 script；與 C# 方法共用同一套語意檢索機制（`domain="view"`） | — |

**落地驗證（grounding，自動、0 token）**：`RUN_AI=true` 跑完答案後，程式化比對答案裡提到的程式/SP/資料表是否真的存在於分析資料，找不到的列在報告末尾「⚠️ 落地驗證」區塊；完全不呼叫 LLM。

**建議組合**：
- 平時檢視（省 token）：`RUN_AI=false`、`SEE_AI_PROMPT=true`、`INCLUDE_SP_DEFS=true`、`USE_CODE_EMBEDDING=true` → 先看「送給 AI 的完整資料」對不對。
- 要正式產答案：把 `RUN_AI=true` 打開；需要最高命中率時加 `USE_CODE_ANNOTATION=true`。

---

## 一次性維護工具

下列工具均在 **Impact 專案目錄**、用 **Impact 的 venv** 執行，**不需要啟動 Impact 服務**、也不連線 SQL Server。只在升級到對應版本之後對「舊快取」跑一次，之後的 `refresh_sql_cli` 都會自動做對。兩支都支援 `--dry-run`（只列出會做什麼）與 `--cache-root`（指定快取根目錄）。

| 工具 | 做的事 | 什麼時候跑 |
|------|--------|------------|
| `python tools\backfill_object_location_indexes.py` | 幫磁碟上每份已存在的 SQL 快取補寫 `<cache_key>.index.json`（Object Location Index）。只讀快取、不改快取內容，重跑等冪 | 升級到有 Object Location Index 的版本之後一次；不跑只會慢（每次反查都整份讀），不會錯 |
| `python tools\repair_sql_execution_graphs.py` | 拿快取裡本來就正確的定義文字重建 `sql_execution_graph`，修正舊寫法腔掉的 offset | 升級到 `graph_version 3` 之後對舊快取一次 |
| `python tools\migrate_sql_cache_keys.py` | 把舊的 `{system_id}__{schema}` 快取檔名改成 `{server}__{database}__{schema}`（ADR-0009），只改名不重掃 | 已經跑過就不必再跑；舊環境還沒搬過才需要 |
| `python -m tools.export_openapi_schema` | 從 `service.api:app` 導出 `docs/openapi/openapi.json`；`--check` 只比對不寫檔 | **新增或修改任何 HTTP 路由／回應欄位之後必跑**，否則測試會失敗 |

> 💡 Object Location Index 本身就是「寧可多報、不可少報」：索引過期、讀不到、或版本不符時一律降級成 `unindexed`（呼叫端整份讀），不會因此產生錯的答案——所以這些工具沒跑完也可以先上線。見 `docs/adr/0012-object-location-index-authoritative-pruning.md`。

---

## Coverage Report：量測分析到底走到哪裡

`coverage_report` 逐一報出每一個 System 的**兩個**比例，永遠不合成一個數字：

| 比例 | 意義 |
|------|------|
| `executed_procedure_name` | Database Invocation 解出 Executed Procedure Name 的比例 |
| `resolved_connection_source` | Database Invocation 解出 Resolved Connection Source 的比例 |

分開報是因為實測過的 repository 卡在相反的地方——有的解不出程序名稱、有的解不出連線來源——合成一個數字只會把「卡在哪裡」藏起來。線以下的每一筆都計入一個具名理由代碼；一個比例數字本身分不出「解析變好了」與「只是變得有自信」。

```powershell
cd d:\pratice\Python\Impact_analysis_system
python -m tools.coverage_report --system Y-Docs_TTPUR
python -m tools.coverage_report --system IQCS --format json --output review\coverage.json
python -m tools.coverage_report --root data\repos\System_Dept_1\Y-DOCs\ATV --cached-only
```

有 current scan cache 時直接重用，所以同一個 System 可以反覆量測而不必重掃；沒有快取時才會掃一次。加 `--cached-only` 連那一次都不做，改成把該掃描根目錄回報成 `scan_cache_missing`／`scan_cache_stale`／`scan_cache_invalid`——「沒量到」和「量到零」永遠不會讀起來一樣。WebForms 系統由同一個指令量測，所以它的數字可以直接跟既有基準比對。

常見理由代碼：

| 理由代碼 | 意義 |
|----------|------|
| `command_text_method_parameter` | 指令文字跨方法邊界傳進來，刻意不做跨方法推導（ADR-0020） |
| `command_text_conflicting_assignments` | 同一方法內有兩個互相衝突的字面指定，不擇一 |
| `wrapper_source_unavailable` | external wrapper 沒有已審核的 contract，見上方 contract 維護 |
| `no_semantic_model` | 該專案沒有語意模型（Semantic Binding Availability 非 `available`） |
| `no_project_connection_scope` | 原始檔上方沒有專案檔，因此沒有連線查找表（ADR-0018） |
| `root_configuration_namespace_not_connection_strings` | 查找鍵讀自 Configuration 根命名空間，兩個命名空間不合併（ADR-0008） |
| `context_type_not_registered` | 組合根沒有註冊這個 DbContext 型別 |
| `no_connection_expression` | 呼叫點本身沒有連線運算式（連線開在看不到的 external wrapper 裡） |

---

## External wrapper contract 維護

外部 wrapper（第三方 library 封裝的 DB 存取）需要人工建立/審核 contract 才能被正式判定為 SQL 呼叫；以下兩個工具用來盤點與審核，**只在新增第三方 library、調整 parser 或 contract registry、定期維護 review 時**執行，不需要每次問答都跑。

### Step A｜盤點（唯讀）

`discover_external_wrappers` 讀取 `data/scan_cache/` 裡的 `db_invocations`，分組並比對 `config/external_wrapper_contracts.json`，**不會** clone、pull 或重新解析。應在 `refresh_cli` **之後**執行（先有 current scan cache 才盤點得到東西）：

```powershell
cd d:\pratice\Python\Impact_analysis_system
python -m tools.discover_external_wrappers --system Y-Docs_TTPUR
```

重點狀態：

| 狀態 | 意義 |
|------|------|
| `source_wrapper` | wrapper 原始碼在本機可追蹤，不需 external contract |
| `auto_selected` / `explicit_selected` | 已唯一對應已審核的 reusable contract，自動/明確套用 |
| `ambiguous_contract` | 一個 receiver type 對到多個 contract，需整理 registry |
| `unresolved_contract`／`unresolved_method`／`receiver_mismatch` | 需人工 review |
| `scan_cache_stale`／`scan_cache_missing` | 不是 contract 問題；先回去跑 `refresh_cli` |

### Step B｜審核並套用

找到需要人工介入的 wrapper 後，依實際查到的 external library 實作整理出 proposal JSON（至少含 `receiver_types` 與 `implementation_snapshot`），用 `accept_external_wrapper_contract.py` 審核：

```powershell
# 1) 先不帶 --apply：只看 diff/reclassification 預覽，不寫任何檔案
python -m tools.accept_external_wrapper_contract --proposal review\proposal.json --system-id Y-Docs_TTPUR --selector sqlobject --scan-root data\repos\System_Dept_1\Y-DOCs\TTPUR

# 2) 確認 diff 符合預期後，加 --apply 正式套用
python -m tools.accept_external_wrapper_contract --proposal review\proposal.json --system-id Y-Docs_TTPUR --selector sqlobject --scan-root data\repos\System_Dept_1\Y-DOCs\TTPUR --apply
```

不帶 `--apply` 一定是安全的預覽，即使 proposal 內容有誤也不會寫入任何檔案。此工具**只改本機檔案，不會建立 git commit**，仍需自行 review 並提交。

> `refresh_cli`（完整 refresh、該 system 有 `database`、preflight 判定 `created`/`reused` 時）也會**自動**把 staged contract 寫入同一組檔案——那是「自動偵測、自動沿用/建立」；這裡是「人工審核、明確套用」，用於 `conflicted`／`preflight_failed` 或需要人工指定 `--selector` 的情境。兩者觸發時機互相獨立，但都經由同一個 `commit_staged_contract_transaction()` 寫入：先寫 transaction manifest、依序替換兩個檔案並驗證 hash，任一步失敗即 rollback；`--apply` 失敗時的錯誤代碼從 `commit_failed`/`commit_rollback_failed` 回報（皆為 `ContractAcceptanceError`），manifest 只在真正需要寫入時才建立，並記錄 `trigger="manual_acceptance"` 以便和 `refresh_cli` 產生的 `trigger="refresh"` manifest 區分。見 [流程圖_進階.md](./流程圖_進階.md)。

---

## 常見問題（進階）

| 症狀 | 處理 |
|------|------|
| `find_by_table` 的 `write_only=true` 沒過濾到預期的寫入 SP，或回 HTTP 409 | 確認請求有帶 `database=<system_id>`，並重新執行 `refresh_sql_cli --system_id <system_id>`（或直接給資料庫名稱）產生通過驗證的 Graph；CLI 模式 3（資料表反查）目前不暴露 `write_only`，需直接呼叫 API 或用 agent 模式的 `search_specs_by_table` |
| `refresh_sql/status/{job_id}` 顯示 `completed` 但正式查詢仍回 409 | `completed` 只代表 worker 正常結束，不代表通過 readiness 驗證；檢查 database scope、Graph version 與 cache version，必要時重跑 `refresh_sql_cli` |
| 不確定哪些系統忘記 clone/掃描/更新 SQL 快取 | 執行 `python tools\check_data_status.py` |
| `discover_external_wrappers` 只回報 `scan_cache_missing`/`scan_cache_stale` | 先跑 `refresh_cli`，這個工具本身不會補做掃描 |
| `coverage_report` 的某個 System 兩個比例都是 `n/a` | 代表「沒量到」不是「量到零」；看同一列的 `not measured:` 理由（快取狀態或 `source_root_unresolved`） |
| `/locate_object` 把每個資料庫都放進 `unindexed`，剪枝完全沒效 | 這些快取還沒有 Object Location Index；跑 `python tools\backfill_object_location_indexes.py` 補建（見上方一次性維護工具） |
| `/locate_object` 回 HTTP 400 | `kind` 只接受 `sp` 或 `table`，不會被猜成其中一種 |
| 新增了路由後 `tests/test_export_openapi_schema.py` 失敗 | 跑 `python -m tools.export_openapi_schema` 重新導出 `docs/openapi/openapi.json` |
| SP 完整定義或 FK 連動表回傳比以前少 | 兩条即時查詢 fallback 已移除（ADR-0011）；快取沒有就是沒有，請重跑 `refresh_sql_cli`，不要重試 |

---

## 延伸閱讀

`docs/` 底下還有幾份較深入的設計/規格文件（不在日常操作範圍，需要時再開）：

- `INTEGRATION_DESIGN.md` — 與 spec-rag 整合的設計
- `C_SHARP_ANALYSIS_GATEWAY_SPEC.md` — Gateway evidence contract 規格
- `GRAPH_GATEWAY_CONTRACT_SPEC.md` — Graph/Gateway 契約規格
- `SQL_EXECUTION_GRAPH_DESIGN.md` — SQL Execution Graph 設計
- `SQL_EXECUTION_PATH_SELECTION_SPEC.md` — Execution Path 選擇規格
- `EXTERNAL_WRAPPER_REFRESH_RECONCILIATION_SPEC.md` — wrapper reconciliation 規格
- `PUR_CSharp_Analyzer_Example.md` — C# 解析範例
- `openapi/openapi.json` — 自動導出的 HTTP API 完整 schema（不手改）
- `adr/` — 架構決策紀錄（ADR）；近期两份：`0011-remove-live-query-fallbacks.md`（移除 SP／FK 的即時查詢 fallback）、`0012-object-location-index-authoritative-pruning.md`（索引新鮮就權威，過期只會變慢不會變錯）
- `agents/` — agent 相關文件

---

## 設計重點

| 設計 | 說明 |
|------|------|
| **兩專案分工** | `llamaindex-spec-rag` 負責理解問題、檢索規格書與呼叫 AI；`Impact_analysis_system` 全程無 AI，只做確定性靜態分析，責任邊界清楚、可各自獨立測試 |
| **Azure DevOps clone-only 來源政策** | 一律以 catalog 的 `azure.{project,repo,branch,path}` clone/沿用，不會退回讀取任意本機路徑；`repo` 為空直接報錯 |
| **持久化掃描快取 + 版本化失效** | `scan_store.py` 一次掃描、之後秒回；`_CACHE_VERSION` 遞增時舊快取自動失效重掃，不需手動清快取 |
| **多框架解析器、依偵測動態啟用** | `ProjectTypeDetector` 偵測框架後只啟用對應解析器，單一檔案解析失敗不影響其餘掃描 |
| **View 層欄位擷取＋GridView 分組** | 擷取畫面實際顯示文字、查詢條件、前端驗證 script，讓「畫面顯示哪些欄位」可直接引用；預設關閉，需 `INCLUDE_VIEW_LAYER=true` |
| **跨程式呼叫參照展開** | `reference_expander.py` 自動把「被分析程式呼叫到、但定義在其他檔案」的方法一併帶入，類似 IDE 的「跟隨參照」 |
| **SQL Execution Graph 正式來源** | `refresh_sql_cli` 建立並寫入 `data/sql_cache/`；`sql_cache_store.py` 以版本、graph version 與 database scope 驗證，缺失/過期時正式 API 回 409，不猜測 relationship |
| **Evidence contract：proven / likely / unresolved** | 只有 `proven` 能形成 confirmed relationship；`likely` 只作 candidate/risk evidence；`unresolved` 必須保留 reason/target。LLM 不能把 `likely`/`unresolved` 改寫成 `proven` |
| **Path Evidence boundary** | `execution_path_builder.py` 只產生 compact path candidate；`/path_evidence` 才展開完整 source-backed 細節。未展開的 path 是 uncertainty，不代表不存在 |
| **`.env` 強制覆寫呼叫端參數** | `RUN_AI`/`SEE_AI_PROMPT` 一律以 spec-rag 的 `.env` 為準，避免不同呼叫路徑行為不一致 |
| **以 SP／資料表反查程式** | `/find_by_sp`、`/find_by_table` 透過 Gateway 與 database-scoped Graph 比對正式 invocation；`cache_only=True`（預設）未 clone/掃描過即直接 `skipped`，避免逐 catalog 系統投機呼叫觸發昂貴 clone |
| **Object Location Index：只會變慢，不會變錯** | 每份 SQL 快取旁邊一張 `<cache_key>.index.json`，記錄該快取能回答哪些 SP／資料表名稱。`/locate_object` 只看索引、不開快取；索引新鮮且不持有該名稱的資料庫才能被剪枝，缺席／過期／版本不符一律降級成 `unindexed`（呼叫端整份讀）。建立時寧可多報不可少報（ADR-0012） |
| **不再偵偵回退去連資料庫** | `sp_fetcher.py`／`fk_resolver.py` 的即時查詢 fallback 已移除（ADR-0011）：快取沒有的就回報沒有，不會用一個您沒要求的預設資料庫默默補上答案。修法是重跑 `refresh_sql_cli`，不是重試 |
| **同一系統可橫跨多個子資料夾** | `azure.path` 支援陣列，`repo_manager` 逐一解析每個子路徑再合併成單一邏輯掃描結果，下游比對邏輯不需感知這是多資料夾組成 |
| **External wrapper contract 兩條寫入入口，一種寫入方式** | `refresh_cli` 完整 refresh 時自動偵測沿用/建立；`accept_external_wrapper_contract.py` 是人工審核觸發的入口；兩者「何時觸發」互相獨立，但都經由同一個 `commit_staged_contract_transaction()` 完成 atomic two-file commit，只有 manifest 裡的 `trigger`（`refresh` vs `manual_acceptance`）不同 |
| **Contract registry/catalog 的 atomic two-file commit** | `contract_transaction.py` 把 registry 與 catalog 視為需一起成功的兩個檔案，任一步失敗即 rollback，不會出現「registry 已更新、catalog 還是舊的」的 mixed state |
| **資料整理狀態一鍵盤點** | `tools/check_data_status.py` 逐系統比對五項狀態，自動列出未完成項目對應的補跑指令 |
