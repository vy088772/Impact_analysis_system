# ========================================
# 核心套件（必需）
# ========================================
beautifulsoup4==4.12.3    # HTML/ASPX 解析
sqlparse==0.4.4           # SQL 解析
pandas==2.2.0             # 資料處理
openpyxl==3.1.2           # Excel 讀寫

# ========================================
# 視覺化（必需）
# ========================================
networkx==3.2.1           # 依賴關係圖
matplotlib==3.8.2         # 基本繪圖

# ========================================
# 工具套件（必需）
# ========================================
tqdm==4.66.1              # 進度條
colorama==0.4.6           # 彩色終端輸出
python-dotenv==1.0.0      # 環境變數

# ========================================
# 資料庫連線（必需）
# ========================================
pyodbc==5.0.1             # MS SQL 連線

# ========================================
# 增強套件（可選）
# ========================================
# 只有在需要高效能 XML 解析時才安裝
# lxml==5.1.0             

# 只有在需要進階圖形視覺化時才安裝
# graphviz==0.20.1        

# 正規表達式增強
regex==2023.12.25

# ========================================
# HTTP 服務（FastAPI 影響分析服務）
# ========================================
fastapi>=0.110           # HTTP API 框架（Python 3.14 需較新版）
uvicorn>=0.29            # ASGI 伺服器
pydantic>=2.6            # 資料模型驗證

# 安裝指令：
pip install beautifulsoup4 sqlparse pandas openpyxl networkx matplotlib tqdm colorama python-dotenv pyodbc regex fastapi uvicorn


