# Likely Evidence Contract

label: wayfinder:grilling
parent: Graph/Gateway Spec Handoff
status: open
assignee: unassigned
blocked_by: none

## Question

`likely` invocation 要如何在 Gateway、SP reverse lookup、formal relationship output 與 migration report 間保持同一套 evidence、reason、database attribution 與 caller/source 欄位語義，才能展示 candidate 而不升級成 confirmed call？
