# 02 — Detect definition-text drift before slicing

**What to build:** Give SQL-side evidence expansion the same staleness protection the C# side already has (`source_snapshot_hash`). Before slicing any operation's SQL fragment out of its module's `definition` text, the system checks whether that text is still the same length it was when the offset was recorded — if it isn't, evidence expansion fails loudly with `stale_path` instead of silently returning text sliced from the wrong position. This closes the failure mode `01` alone cannot: an operation early enough in a stored procedure that a drifted offset still lands inside the current text's bounds and returns wrong-but-valid-looking content with no error at all.

**Blocked by:** 01 — touches the same graph-node construction code `01` fixes; landing after it keeps the node-schema changes in one coherent order and avoids test fixtures exercising two different bugs at once.

**Status:** done

- [x] Each `dml_operation`/`unresolved_dynamic_sql` node gains a recorded length of its module's `definition` text at the moment the graph is built.
- [x] Before any slice is attempted, evidence expansion compares that stored length against the module's current `definition` length as currently loaded; a mismatch raises `PathEvidenceError("stale_path", ...)` with a reason naming the mismatch, and no slice is attempted.
- [x] A new test extending `tests/test_exact_path_evidence.py`'s `test_path_evidence_rejects_stale_source_snapshot` pattern constructs a fixture where the stored length and the current `definition` length disagree — no real corrupted cache needed, a hand-built mismatch is sufficient — and asserts `PathEvidenceError.code == "stale_path"`.
- [x] The check does not change behavior for any node whose stored length matches its module's current `definition` length — all existing passing evidence-expansion tests continue to pass unmodified.

## Comments

- `service/sql_execution_graph.py`: `build_sql_execution_graph` now passes `len(definition)` into `_add_operation`, which stamps it as `source["module_definition_length"]` alongside the existing `start_offset`/`length` fields — applies uniformly to both `dml_operation` and `unresolved_dynamic_sql` nodes since both flow through the same code before the node is added.
- `service/analyze_service.py`'s `_materialize_path_evidence`: before the existing `_slice_utf16` call, compares `source["module_definition_length"]` against the module's current `definition` length; on mismatch raises `PathEvidenceError("stale_path", ...)` naming both the recorded and current lengths, before any slice is attempted. The comparison is skipped (not an error) when a node has no recorded length at all — i.e. a graph built before this change — so already-cached graphs degrade to the old (unguarded) behavior rather than crashing; issue 03 owns forcing those graphs to repair via a `GRAPH_VERSION` bump.
- Added two tests in `tests/test_exact_path_evidence.py`: `test_path_evidence_rejects_drifted_sql_definition_offset` (hand-built length mismatch on the terminal operation's `source`, in-bounds offset, asserts `PathEvidenceError.code == "stale_path"`) and `test_path_evidence_accepts_matching_sql_definition_offset` (matching recorded/current length, asserts the slice still succeeds normally — proves checklist item 4's no-regression claim).
- `/code-review` (Standards + Spec axes) ran clean of hard violations. Standards axis noted two judgement calls only: `_add_operation`'s parameter list is a pre-existing Data Clump that grew by one primitive (not addressed — matches existing call shape), and the two new tests repeat existing three-line monkeypatch boilerplate (consistent with this file's house style, not addressed). Spec axis flagged that `int(recorded_length)` wasn't guarded against a non-numeric value in a corrupted cache, which would have raised a raw `ValueError` instead of `PathEvidenceError` — fixed by wrapping that conversion in its own `try/except (TypeError, ValueError)` that re-raises as `PathEvidenceError("stale_path", ...)`. Spec axis also confirmed no scope creep and no missing requirements beyond the deliberately-deferred-to-03 point above.
- Full test suite run: `tests/test_exact_path_evidence.py` + `tests/test_sql_execution_graph.py` — 20 passed. Broader `tests/` run: 512 passed, 11 pre-existing failures and 2 pre-existing collection errors reproduce identically with this change stashed out (same set issue 01 documented: missing ODBC driver, hardcoded Windows/`d:\` paths, unrelated wrapper-evidence fixtures) — unrelated to this ticket.
