# 02 — Collapse the 16 non-evidence wrapper-evidence alias groups at the domain seam

**What to build:** `WrapperReconciliation.to_dict()` is replaced by a standalone `project_wrapper_evidence(reconciliation) -> dict` function that emits exactly one canonical key per fact for the 16 true-duplicate groups (everything in the spec's canonical-name table except `evidence`/`evidence_status`, which is handled separately in ticket 03). `WRAPPER_EVIDENCE_FIELDS` shrinks to match. This is the seam every wrapper-evidence consumer in this repo goes through.

**Blocked by:** None — can start immediately.

**Status:** resolved

- [x] `project_wrapper_evidence(reconciliation: WrapperReconciliation) -> Dict[str, Any]` is added next to `WrapperReconciliation` in `code_analyzer/csharp_analysis_gateway.py`, replacing `to_dict()`. `WrapperReconciliation` itself keeps only typed attributes — no serialization method remains on the class.
- [x] `project_wrapper_evidence`'s output uses exactly one canonical key for each of these 16 groups, per the spec's table: `status`, `selection_source`, `contract`, `contract_mode`, `contract_sink`, `candidate_contracts`, `scan_root`, `source_available`, `review_candidate`, `classification_reason`, `mode_reason`, `wrapper_method`, `stored_procedure_mode`, `contract_signature_version`, `contract_lifecycle_status`, `source_snapshot_hash` (including inside the nested `source_provenance` object). None of the dropped aliases (`wrapper_status`, `wrapper_classification_status`, `classification_status`, `wrapper_selection_source`, `wrapper_contract`, `selected_contract`, `wrapper_contract_mode`, `wrapper_contract_sink`, `wrapper_contract_candidates`, `candidate_contract_names`, `wrapper_scan_root`, `wrapper_source_available`, `wrapper_review_candidate`, `wrapper_unresolved_reason`, `wrapper_mode_reason`, `observed_method`, `wrapper_stored_procedure_mode`, `signature_version`, `contract_status`, `source_snapshot_identity`) appear in the output.
- [x] The `evidence`/`evidence_status` pair, all ten dual-fact identity/receiver/method pairs (`implementation_identity`/`wrapper_implementation_identity` and friends, `receiver_type`/`wrapper_receiver_type`, `external_wrapper_method`/`wrapper_method`), and every already-single-named field are left untouched and continue to be emitted exactly as today.
- [x] `WRAPPER_EVIDENCE_FIELDS` in `code_analyzer/csharp_analysis_gateway.py` is updated to contain exactly the canonical key set now produced (dropping the 20 alias entries covered by this ticket's scope; the `evidence`/`evidence_status` entries are left alone for ticket 03 to handle).
- [x] `analyze_service.py`'s `_wrapper_projection_fields` and `invocation_wrapper_evidence_fields` continue to work unchanged in shape against the shrunk `WRAPPER_EVIDENCE_FIELDS`.
- [x] `tests/test_external_wrapper_contract_identity.py`, `tests/test_external_wrapper_discovery.py`, and `tests/test_csharp_analysis_gateway.py`'s existing calls to `.to_dict()` are updated to call `project_wrapper_evidence(...)`; any assertion on a now-dropped alias key is rewritten to assert the canonical key. (`tests/test_exact_path_evidence.py` also asserted directly on dropped aliases via `PathEvidenceResponse`; rewritten to canonical keys too, since it wasn't safe from the field-list shrink even though it wasn't in the ticket's named list.)
- [x] A new focused test (`test_project_wrapper_evidence_drops_all_alias_keys` in `tests/test_csharp_analysis_gateway.py`) constructs a `WrapperReconciliation` and asserts `project_wrapper_evidence(...)` contains none of the 20 dropped alias keys listed above.
- [x] Full existing test suite passes (same 11 pre-existing, unrelated failures as on `main` before this change — see Further Notes).

**Known scope boundary (not a defect in this ticket):** `project_wrapper_evidence`'s own output (from `WrapperReconciliation`) has no `source_provenance` object at all — that nested dict is only ever built by `wrapper_observation_fields`/`invocation_wrapper_evidence_fields`, which this ticket's own checklist item 3 explicitly requires to keep "unchanged in shape." Those functions still write both `source_snapshot_hash` and `source_snapshot_identity` into the nested `source_provenance` dict (and the raw top-level dict), because `tests/test_program_refresh.py::test_review_candidates_keep_each_source_snapshot_identity` asserts on that raw, unfiltered dict and would break if the alias were stripped there. The alias is fully gone from every response that goes through the `WRAPPER_EVIDENCE_FIELDS`-filtered path (`/path_evidence`, `/refresh` `SPMatchProgram`/`TableMatchProgram` projections), but still leaks inside the nested `source_provenance` object on any consumer that reads the raw `wrapper_observation_fields` dict directly (e.g. `refresh_source`'s `review_items`) and inside `service/schemas.py`'s still-independently-declared `source_snapshot_identity` field. Closing this gap requires touching `wrapper_observation_fields` and the raw refresh-summary consumers, which is out of this ticket's declared boundary — flag for a follow-up (likely alongside ticket 04's `service/schemas.py` consolidation, which already owns that duplicate field declaration).

**Further Notes:** The 11 failures left in the suite (`test_resolved_invocation_retains_wrapper_reconciliation_provenance`, `test_external_wrapper_contract_requires_explicit_selection_even_for_unique_receiver_type`, `test_static_analyzer_host_applies_external_sqlobject_wrapper_contract`, `test_sqlobject_receiver_with_configured_contract_resolves_explicitly`, `test_report_groups_calls_and_preserves_source_locations`, `test_report_evidence_and_provenance_match_refresh_reconciliation`, `test_dependency_graph_renderer_ignores_legacy_sp_relations`, `test_wrapper_projection_matches_analyze_and_reverse_lookup_surfaces`, `test_mvc_project_scan`, `test_refresh_selector_does_not_prove_database_evidence`, `test_refresh_reconciliation_uses_database_scoped_sp_catalog`) reproduce identically on the pre-change branch (verified via `git stash`) — pre-existing environment/StaticAnalyzerHost issues, not caused by this ticket. `tests/test_search_roles.py` and `tests/test_sp_tables.py` fail collection on this machine for an unrelated reason (no ODBC driver for a live DB connection), also pre-existing.
